package com.example.mycarsmt.presentation

import androidx.fragment.app.FragmentManager

class FragmentHelper(val manager: FragmentManager) {
    //здесь должны быть все методы перехода по фрагментам из MainActivity
}